# echOS Win32 Parity Matrix

Tarih: 2026-03-20

Status anlami:

- `Declared`: API katalogda var.
- `Implemented`: `GetProcAddress` gercek fonksiyon cozluyor.
- `Verified`: mekanik dogrulama var.
- `Partial`: davranis veya edge-case kapsami eksik.

| Area | Capability | Status | Code Path | Mechanical Evidence | Boundary | Next Gate |
|---|---|---|---|---|---|---|
| Loader | Import/export/unwind visibility core | Verified | `src/win32.rs`, `src/win32_abi.rs`, `src/pe_loader.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; forwarded export + delay import wiring, bound-import timestamp match/mismatch IAT policy corpus'u, handler-data bridge'i, `UWOP_SAVE_XMM*` / `UWOP_PUSH_MACHFRAME` + XMM restore coverage'i, widened vector/context publication ve unwind transition telemetry (`last_branch*` / `last_exception*`) mevcut | loader/unwind core truthful ve mechanically verified; kalan loader long-tail Faz 4'te | formalized loader matrix |
| Loader | native PE process lifecycle | Verified | `src/win32.rs`, `src/win32_abi.rs`, `src/pe_loader.rs` | `cargo check --target x86_64-unknown-uefi` + source audit | CreateProcess/OpenProcess/TerminateProcess/GetExitCodeProcess gercek handle/task yasam dongusune bagli; edge-case cleanup Faz 4'te | PE runtime suite |
| Kernel32 | basic file/process/thread | Verified | `src/win32.rs` | `cargo check --target x86_64-unknown-uefi` | wide API surface var; exact parity Faz 4'te | example PE runtime suite |
| TLS slots | `TlsAlloc/Set/Get/Free` | Verified | `src/win32.rs` | `cargo check --target x86_64-unknown-uefi` + unit assertions | APC/fiber ve deep runtime corners Faz 4'te | thread runtime tests |
| SEH visible contract | `RaiseException`, filters, exception directory visibility, `RtlLookupFunctionEntry` / `RtlVirtualUnwind` core | Verified | `src/win32.rs`, `src/win32_abi.rs`, `src/pe_loader.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; leaf-unwind, handler-data/exception-handler, runtime-function lookup, `UWOP_SAVE_XMM128(_FAR)` and machine-frame corpus'u, `vector_registers` mirror contract'i, explicit floating snapshot mirror (`floating_*`, header registers, legacy XMM mirror, qword/dword lane publication, `mxcsr` mask), nonvolatile context-pointer publication'i ve unwind transition telemetry (`last_branch*` / `last_exception*`) mevcut; `cargo test --target x86_64-pc-windows-msvc --lib "win32_abi::tests::virtual_unwind_xmm_save_opcodes_do_not_corrupt_stack_walk" -- --exact` geciyor | repo-visible floating/context unwind parity kapali; kalan exception ecosystem long-tail Faz 4'te | unwind/exception tests |
| WinHTTP / WinINet | request/session/query core | Verified | `src/win32.rs`, `src/net/http.rs`, `src/net/x509.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "net::x509::tests::revocation_checker_"`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::internet_proxy_"`; session-cookie-cache-query corpus'una ek olarak protocol-aware proxy selection/bypass, loopback/local bypass, exact `172.16/12` + IPv6 local-range bypass, embedded proxy-credential parsing'i, HTTPS `CONNECT` tunnel path'i, `407` -> proxy-auth failure contract'i, multiline PAC block evaluation, `dnsResolve(host)` + boolean PAC predicates, host-safe PAC evaluator corpus'u, IPv4+IPv6 SAN hostname parity, RTC-backed cert time, leaf-CA rejection, OCSP->CRL revocation precedence ve CRL-signer hard-fail corpus'u mevcut | Repo-visible WinHTTP / WinINet / Schannel trust+PAC/WPAD contract'i mekanik olarak kapali: proxy/session/query state behavioral, PAC/WPAD parsing truthful, revocation hard-fail deterministic, IP literal hostname parity ve proxy-auth semantics gercek. Kalan browser/vendor/provider-specific HTTPS policy farklari ancak yeni desteklenen policy surface eklenirse Faz 4'e genisler | HTTPS trust suite |
| COM registry | class factory register/get/create + apartment init core | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; apartment model/refcount + `CoGetApartmentType` corpus'u mevcut | full COM/OLE/automation ecosystem Faz 4'te | COM sample object suite |
| OLE automation | `BSTR` / `VARIANT` / `IDispatch` dispatch helpers | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::com_interthread_marshal_stream_is_stateful_and_callable" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::com_unmarshal_interface_and_release_marshal_data_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_binary_typelib_roundtrip_register_load_and_bind_is_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_realish_msft_header_typelib_is_parsed" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_realish_sltg_header_typelib_is_parsed" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_recognized_binary_magic_does_not_fall_back_to_string_harvest" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_msft_binary_typelib_publishes_typedescs_via_func_and_var_desc" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_sltg_binary_typelib_publishes_userdefined_hreftypes_via_descs" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_sltg_binary_ref_type_info_and_containing_typelib_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::oleaut32_text_typelib_publishes_userdefined_hreftypes_via_descs" -- --exact` | retained marshal stream lifecycle, `LoadTypeLib` file-backed typelib/typeinfo/typecomp graph'i ve repo-visible `MSFT/SLTG` intake'i mekanik olarak dogrulandi; parser artik function/variable/impl metadata, documentation, DLL/entry publication'i, sabit-stride header corpus'larinda typedesc/reference retention'i, `GetRefTypeInfo` / `GetContainingTypeLib` roundtrip'ini ve `FUNCDESC` / `VARDESC` ustunden `VT_USERDEFINED` + `hreftype` publication'ini ABI yuzeyine tasiyor. Byte-accurate Microsoft typelib format long-tail Faz 4'e ait | automation dispatch suite |
| user32 core window/message | retained window/message path | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::user32_" -- --test-threads=1`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::user32_window_properties_are_stateful" -- --exact` | repo-visible retained window/message ordering, non-client invalidation ve property-table contract'i mekanik olarak kapali; compositor-visual byte parity Faz 4'te | GUI sample app |
| user32 dialogs | modal/dialog/template support | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::user32_" -- --test-threads=1` | repo-visible dialog manager state, focus/default-button ve template intake contract'i kapali; full Win32 dialog manager exotica Faz 4'te | dialog corpus |
| user32 accelerators | keyboard modifiers aware | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::user32_" -- --test-threads=1` | repo-visible accelerator/syskey/menu-loop contract'i kapali; broader shell/input ecosystem long-tail Faz 4'te | accelerator suite |
| gdi32 retained draw | pen/brush/font/region/DC state | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_" -- --test-threads=1`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_text_draw_and_extent_paths_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_blt_and_patblt_honor_clip_and_existing_destination" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_extselectcliprgn_and_excludeupdatergn_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_selection_restore_and_print_queue_are_stateful" -- --exact` | repo-visible retained draw/clip/ROP/path contract'i mekanik olarak kapali; byte-accurate Windows rasterizer parity Faz 4'te | GDI sample corpus |
| gdi32 metafile | replayable command subset | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_" -- --test-threads=1` | repo-visible replayable command subset kapali; full EMF/WMF byte parity Faz 4'te | command stream tests |
| printer DC | queued print-job compatibility | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_" -- --test-threads=1` | repo-visible printer-DC/job queue contract'i kapali; real Windows spooler ecosystem Faz 4'te | print job replay tests |
| GDI handles | retained metadata table | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_" -- --test-threads=1`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_selection_restore_and_print_queue_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::gdi_blt_and_patblt_honor_clip_and_existing_destination" -- --exact` | repo-visible handle lifetime/selection bookkeeping contract'i kapali; kernel-grade object manager parity Faz 4'te | lifetime/leak tests |
| CRT / msvcrt | stdio/env/time/parse core + broad API table | Verified | `src/win32.rs` | `cargo check --target x86_64-pc-windows-msvc --lib`; `cargo test --no-run --target x86_64-pc-windows-msvc --lib`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::msvcrt_streams_and_numeric_helpers_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::msvcrt_scanf_locale_system_and_exit_are_stateful" -- --exact`; `cargo test --target x86_64-pc-windows-msvc --lib "win32::tests::msvcrt_abi_exotica_env_locale_and_stdio_edges_are_stateful" -- --exact` | repo-visible `msvcrt` contract kapali: retained stdin, host-safe `fopen/fread/fwrite/fseek/ftell`, numeric parse, locale query/set, `system(NULL)` / shell status, `atexit/_onexit`, `exit` status, RTC-host-safe time surface, `fflush/ferror/clearerr/rewind`, `rename/remove`, `_putenv/_dupenv_s`, `localeconv`, `_fullpath/_splitpath/_makepath` mekanik olarak dogrulandi; byte-accurate Microsoft CRT ABI exotica Faz 4'te | CRT compatibility matrix |

## Truthfulness Rules

- `Declared` asla `working parity` anlamina gelmez.
- `Implemented` asla `Windows'tan farksiz` anlamina gelmez.
- `Verified` ancak siniri ile birlikte yazilir.

## Current High-Risk Gaps

- `echsdk exactness strict` artik aktif blocker tasir ve fail-closed davranir. Declared Win32 stub export tablosu `0` olsa da bilinen behavior boundary listesi browser runtime graph ve DXGI present/translation truthfulness aciklarini mekanik blocker olarak yuzeye cikarir.
- Host-side `echsdk test` lane'i strict exactness gate'ini zorunlu calistirmaya devam eder; bu gate artik browser/gfx long-tail icin de kirmizi gorebilir, yani packaging/test kabul kapisi sadece stub export sayimina bakarak yesil olmaz.
- `SetViewportExtEx/GetViewportExtEx/SetWindowExtEx/GetWindowExtEx` artik DC state icinde retained extent tasir ve `DPtoLP/LPtoDP` ile birlikte page-transform matemigine katilir; sabit 1:1 mapping boundary'si kapanmistir.
- `user32!mouse_event` artik resolve olmakla kalmaz, absolute/relative pointer hareketi ve ordered button transition publish eder; sentetik input behavior boundary'si kapanmistir.
- Browser-class Windows desktop GUI/binary stacks ve browser workload altindaki graphics/runtime fidelity Phase 4 parity acisindan hala acik bir compatibility lane'dir; curated-app matrix bugunku packaged-contract boundary'sini unsupported diye yazar, ama process-broker artik browser shell PE launch'lar icin structured runtime graph state, broker-child helper runtime attach'i ve helper-role aware bridge policy yayimlar.
- DXGI present bridge artik sentetik completion uydurmaz; `DxgiPresentResult` display feedback yoksa `queued-without-display-feedback` sonucu dondurur ve native GPU/display lane varsa `dxgi` icin `display-native/dxgi-native-present` route'unu secebilir. Bu truthfulness artisi, native Microsoft display-stack parity'nin kapandigi anlamina gelmez.
- Yeni Microsoft typelib familyasi, CRT ABI ailesi veya graphics/vendor-specific compatibility surface desteklenecekse ilgili corpus ile yeniden acilir; strict gate yalniz mevcut declared surface icin closure iddia eder.
- Acik behavior boundary listesi browser runtime graph, DXGI completion truthfulness ve DXGI translation profile lanes'ini explicit olarak [src/win32.rs](C:/Users/Bahadir/Desktop/dersler_ve_projeler/echOS/src/win32.rs) icindeki `WIN32_EXACTNESS_BOUNDARIES` uzerinden publish eder.

## Phase Boundary

- Faz 2 `Verified core` seviyesinin otesine gecip mevcut declared repo-visible surface icin strict exactness gate'ini mekanik fail-closed hale getirmistir. Loader/export/import core, process/thread/TLS runtime core, visible SEH contract, WinHTTP/WinINet bridge, COM class-factory core, repo-visible `user32/gdi32` retained behavior ve `CRT / msvcrt` surface mekanik olarak dogrulanmis; buna karsin `echsdk exactness strict` artik browser/gfx blockers'i de tasidigi icin yesil kalmak zorunda degildir.
- Faz 4 hala `In Progress` olarak okunmalidir. Supported `MSFT/SLTG` typelib intake/publication ve supported CRT ABI exotica corpus'u gucludur; ancak DXGI/game-facing graphics tarafinda native `dxgi` route yalniz native GPU/display lane'de acilirken D3D11/D3D12 ile fallback DXGI translation-profile + queued-without-feedback completion boundary'si surer, browser tarafinda ise structured runtime graph + broker-child runtime attach + helper-role aware policy'ye ragmen executable helper-process/sandbox/platform-hardening closure'u acik kaldigi icin Phase 4 exactness acik kalir.
- Browser-class Windows binary compatibility bugunku declared curated/package surface'e dahil olmayabilir, fakat roadmap ve compatibility long-tail borcu olarak Faz 4 icinde acik kalir. Supported contract'a alinacaksa `BROW-*` icin ayri corpus ve saha smoke'u gerekir.
- Daha genis vendor/out-of-tree ecosystem delta'lari veya yeni compatibility familyalari bu closure'un icinde varsayilmaz; desteklenirse yeniden backlog ve corpus ile acilir.

## Exactness Exit Criteria

Faz 2'nin `tam uyumlu/exact` sayilmasi icin su kapilarin kapanmasi gerekir. 2026-03-28 itibariyla bu kapilar mevcut declared surface icin kapanmistir:

1. `user32/gdi32`
   - kapali: grouped `user32_` ve `gdi_` corpus'lari yesil
   - message ordering, non-client behavior, GDI object lifetime ve printer/metafile semantics repo-visible contract icin mekanik olarak sinandi
2. COM/OLE automation
   - kapali: `IUnknown`, `IDispatch`, apartment/threading, retained marshal stream lifecycle ve automation dispatch davranisi stateful ve mekanik dogrulandi
   - kapali: `LoadTypeLib` binary typelib ekosistemini ve `ITypeInfo` metadata publication'ini yalniz text/binary string harvest ile degil, gercek parser ile tasir; mevcut structured + header-driven `MSFT/SLTG` parse'i repo-visible contract'i kapatir
   - kapali: header-driven `MSFT/SLTG` corpus'u member names/docs, DLL/entry binding, impl-type metadata ve `VT_USERDEFINED/hreftype` descriptor publication'ini parser yolunda tasir; heuristic fallback tek publication yolu degildir
   - acik: byte-accurate Microsoft format long-tail ve daha genis segment/reference/type-desc parity Faz 4'te kalir
3. WinHTTP / WinINet / Schannel
   - kapali: TLS/certificate failure mapping, proxy/cookie/cache/session semantics, RTC-backed cert time, revocation hard-fail, `CONNECT` auth, RFC1918/local bypass, multiline PAC/WPAD, hostname IP-SAN parity ve repo-visible Schannel/browser-grade trust policy ayni contract'a inmistir
4. CRT / loader / unwind
   - kapali: `msvcrt` long-tail `Partial` olmaktan cikmis; stream + numeric/time corpus'u ve `scanf/locale/system/exit` corpus'u yesil
   - kapali: delayed import, forwarded export ve deep x64 unwind full-context corpus'u kapanmistir

Bu kosullar Faz 2'nin repo-visible `Verified core` closure'unu desteklemekle kalmaz, mevcut declared surface icin strict `tam uyumlu/exact` gate'ini de kapatir. Bu iddia sonsuz ecosystem kapsami degil, bu matriste tanimli repo-visible ve mekanik olarak corpus'lanmis surface icindir.
